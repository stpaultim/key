<?php

/**
 * @file
 * Declares a bogus key provider for testing purposes.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function key_test_ctools_plugin_directory($module, $plugin) {
  if ($module == 'key_test') {
    return 'plugins/' . $plugin;
  }
}
